# LAST Z · CanvasGUI 3.2.0 版

此目录是 `template/index.html` 的 CanvasGUI 迁移版本。

- Three.js 游戏、关卡、武器、成长、存档和音频逻辑保持一致。
- 可见 HUD、菜单、弹窗、按钮、文本输入和移动摇杆绘制在 canvas 上。
- 交互控件由 canvasGUI 3.2.0 管理。
- 不再加载 `tailwindcss-browser.js`。

使用静态 HTTP 服务器打开 `index.html`（不要直接使用 `file://`）。
